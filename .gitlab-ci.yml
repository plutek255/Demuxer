pages:
  stage: deploy
  script:
    - echo "deploying public website to $CI_PAGES_URL, which is viewable to everyone"
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
